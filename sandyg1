#include <stdio.h>

int main()
{
    
	int a;
	scanf("%d",&a);
	if(a>0)
	{
		printf("\n the num is positive");
	}
	else if(a<0)
	{
		printf("the num is negative");
	}
	else
	{
		printf("the num is zero");
	}
	return 0;
}
